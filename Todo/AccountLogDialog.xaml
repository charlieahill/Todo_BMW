<Window x:Class="Todo.AccountLogDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Account Log" Height="450" Width="1000" WindowStartupLocation="CenterOwner" ResizeMode="CanResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" VerticalAlignment="Center" Margin="0,0,0,8">
            <TextBlock Text="From:" VerticalAlignment="Center" Margin="0,0,6,0" />
            <DatePicker x:Name="FromDatePicker" Width="130" />
            <TextBlock Text="To:" VerticalAlignment="Center" Margin="12,0,6,0" />
            <DatePicker x:Name="ToDatePicker" Width="130" />
            <TextBlock Text="Kind:" VerticalAlignment="Center" Margin="12,0,6,0" />
            <ComboBox x:Name="KindCombo" Width="120" />
            <Button x:Name="ApplyButton" Content="Apply" Width="80" Margin="12,0,0,0" Click="ApplyFilter_Click" />
            <Button x:Name="ExportButton" Content="Export" Width="80" Margin="8,0,0,0" Click="Export_Click" />
        </StackPanel>

        <ListView x:Name="LogList" Grid.Row="1">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="Background" Value="White" />
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="LightBlue" />
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Applied Date" Width="140" DisplayMemberBinding="{Binding AffectedDateDisplay}" />
                    <GridViewColumn Header="Kind" Width="100" DisplayMemberBinding="{Binding Kind}" />
                    <GridViewColumn Header="Delta" Width="90" DisplayMemberBinding="{Binding DeltaDisplay}" />
                    <GridViewColumn Header="Balance" Width="90" DisplayMemberBinding="{Binding BalanceDisplay}" />
                    <GridViewColumn Header="Note" Width="400" DisplayMemberBinding="{Binding Note}" />
                    <GridViewColumn Header="Edited Date" Width="180" DisplayMemberBinding="{Binding Date, StringFormat={}{0:yyyy-MM-dd HH:mm}}" />
                </GridView>
            </ListView.View>
        </ListView>

        <StackPanel Orientation="Horizontal" Grid.Row="2" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button x:Name="CloseButton" Content="Close" Width="80" Margin="8,0,0,0" IsCancel="True" />
        </StackPanel>
    </Grid>
</Window>
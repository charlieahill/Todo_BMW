<Window x:Class="Todo.LocationColorsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Physical location colors" Height="420" Width="600" WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <DataGrid x:Name="ColorsGrid" AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False" HeadersVisibility="Column" Grid.Row="0" Margin="0,0,0,8">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Location" Binding="{Binding Location}" IsReadOnly="True" Width="2*"/>
                <DataGridTemplateColumn Header="Color" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Border Width="24" Height="16" BorderBrush="Gray" BorderThickness="1" Background="{Binding ColorBrush}" Margin="0,0,6,0"/>
                                <TextBox Text="{Binding ColorHex, UpdateSourceTrigger=PropertyChanged}" Width="100"/>
                                <Button Content="Pick" Margin="6,0,0,0" Padding="8,2" Click="PickColor_Click" Tag="{Binding}"/>
                                <Button Content="Clear" Margin="6,0,0,0" Padding="8,2" Click="ClearColor_Click" Tag="{Binding}"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Orientation="Horizontal" Grid.Row="1" HorizontalAlignment="Right">
            <Button Content="OK" IsDefault="True" Width="90" Margin="0,0,6,0" Click="OK_Click"/>
            <Button Content="Cancel" IsCancel="True" Width="90" />
        </StackPanel>
    </Grid>
</Window>
